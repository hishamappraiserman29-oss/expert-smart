name: E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  e2e:
    name: Playwright Smoke Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      # Dummy secret so bridge_api.py auth middleware initialises cleanly.
      # E2E tests only exercise the UI (health check + DOM); no JWT is issued.
      JWT_SECRET: ci-test-secret-for-e2e-workflow

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install Python dependencies
        run: |
          pip install --no-cache-dir -r core_engine/requirements.txt
          pip install -r requirements-dev.txt

      - name: Install Playwright browsers
        run: python -m playwright install --with-deps chromium

      - name: Run E2E smoke tests
        run: pytest core_engine/tests/e2e/ -v --tb=short
