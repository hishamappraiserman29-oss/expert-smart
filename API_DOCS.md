# Expert Smart — API Reference

> Auto-generated by PH_5_docs_deploy.py on 2026-05-09

Total routes: **84**

---

## `/api/<path:p>`
**Methods**: `OPTIONS`  
**Handler**: `_pre`  

## `/`
**Methods**: `GET`  
**Handler**: `serve_index`  
**Description**: يُقدّم الصفحة الرئيسية sovereign_brain.html مباشرة على / from flask import send_from_directory # ابحث عن sovereign_brain.html أولاً ثم index.html for fname in ("index.html", "sovereign_brain.html"): f  

## `/<path:path>`
**Methods**: `GET`  
**Handler**: `serve_static`  
**Description**: يُقدّم ملفات CSS/JS/fonts/images من مجلد frontend/ from flask import send_from_directory full = os.path.join(_FRONTEND_DIR, path) if os.path.isfile(full): return send_from_directory(_FRONTEND_DIR, pat  

## `/api/valuation`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_valuation`  

## `/api/download/<filename>`
**Methods**: `GET`  
**Handler**: `download`  

## `/api/market-feed`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `market_feed_post`  
**Description**: يُضيف سجل بيانات سوقية جديد من أي مصدر خارجي. Body (JSON): { source":        "facebook" | "forum" | "agent" | "direct" | "other", location":      "القاهرة الجديدة",  

## `/api/market-feed`
**Methods**: `GET`  
**Handler**: `market_feed_get`  
**Description**: يُعيد ملخص بيانات السوق المُخزّنة مع إمكانية التصفية. Query params: location=      (اختياري) تصفية بالموقع (تطابق جزئي) property_type= (اختياري) تصفية بالنوع since=         (اختياري) تاريخ ISO  e.g. 2  

## `/api/market-feed/<record_id>`
**Methods**: `DELETE`  
**Handler**: `market_feed_delete`  
**Description**: يحذف سجلاً بعينه من قاعدة البيانات try: feed = _load_feed() before = len(feed) feed = [r for r in feed if r.get("id") != record_id] _save_feed(feed) return jsonify({"status": "success",  

## `/api/advisor`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `advisor_endpoint`  
**Description**: المستشار العقاري الذكي — يُجيب على الأسئلة العقارية العامة باستخدام RAG (Qdrant + market_feed + Macro KB + DuckDuckGo + Ollama). Body (JSON): { question":      "ما هو أفضل وقت للاستثمار في رأس الحكمة؟  

## `/api/advisor/health`
**Methods**: `GET`  
**Handler**: `advisor_health`  
**Description**: فحص حالة المستشار الذكي return jsonify({ status":    "ok" if _RAG_OK else "disabled", rag_ready": _RAG_OK, message":   "المستشار الذكي جاهز" if _RAG_OK else "RAG غير متاح — تحقق من المكتبات", })  

## `/api/tune/analyze`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `tune_analyze`  
**Description**: يحلّل نص وثيقة ويستخرج بصمتها الأسلوبية. Body: { "text": "...", "filename": "تقرير تقييم 2024.pdf" } Response: { "status":"success", "profile": {...}, "injection_preview": "..." }  

## `/api/tune/profiles`
**Methods**: `GET`  
**Handler**: `tune_list_profiles`  
**Description**: يُعيد قائمة الأساليب المحفوظة try: return jsonify({"status": "success", "profiles": _tune_list()}) except Exception as e: return jsonify({"status": "error", "message": str(e)}), 500  

## `/api/tune/profiles/<profile_id>`
**Methods**: `GET`  
**Handler**: `tune_get_profile`  
**Description**: يُعيد تفاصيل ملف أسلوب profile = _tune_load(profile_id) if not profile: return jsonify({"status": "error", "message": "الملف غير موجود"}), 404 return jsonify({"status": "success", "profile": profile})  

## `/api/tune/profiles/<profile_id>`
**Methods**: `DELETE`  
**Handler**: `tune_delete_profile`  
**Description**: يحذف ملف أسلوب ok = _tune_delete(profile_id) return jsonify({"status": "success" if ok else "error", deleted": ok}) @app.route("/api/tune/apply", methods=["POST", "OPTIONS"])  

## `/api/tune/apply`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `tune_apply_prompt`  
**Description**: يُطبّق بصمة أسلوب على موجّه أساسي ويُعيد الموجّه المُعزَّز. Body: { "prompt": "...", "profile_id": "abc123" }  

## `/api/library`
**Methods**: `GET`  
**Handler**: `library_list`  
**Description**: يُعيد وثائق المكتبة مع دعم الفلترة والبحث. GET /api/library?q=تقييم&sector=residential&limit=20  

## `/api/library/scan`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `library_scan`  
**Description**: يُشغّل مسح دوري لكل المصادر المُفعَّلة. POST /api/library/scan  

## `/api/library/add`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `library_add_manual`  
**Description**: يُضيف وثيقة مرفوعة يدوياً إلى المكتبة. Body: { "title":"...", "text":"...", "filename":"...", sector":"residential", "doc_type":"valuation_report" }  

## `/api/library/<record_id>`
**Methods**: `GET`  
**Handler**: `library_get`  
**Description**: يُعيد تفاصيل وثيقة rec = _lib_get(record_id) if not rec: return jsonify({"status": "error", "message": "الوثيقة غير موجودة"}), 404 return jsonify({"status": "success", "record": rec})  

## `/api/library/<record_id>`
**Methods**: `DELETE`  
**Handler**: `library_delete`  
**Description**: يحذف وثيقة من المكتبة ok = _lib_delete(record_id) return jsonify({"status": "success" if ok else "error", "deleted": ok}) # ════════════════════════════════════════════════════════════════════════════  

## `/api/price-map`
**Methods**: `POST` / `GET` / `OPTIONS`  
**Handler**: `price_map`  
**Description**: خريطة الأسعار التفاعلية. POST /api/price-map  { "location": "التجمع الخامس", "price_per_meter": 42000 } GET  /api/price-map?format=html&location=...&price_per_meter=... إذا format=html → يُعيد صفحة HT  

## `/api/radar/start`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `radar_start`  
**Description**: يُشغِّل محرك رادار السوق في الخلفية. Body (optional): { interval_seconds": 1800, enable_web": true, enable_whatsapp": false, wa_groups": ["مجموعة عقارات القاهرة"]  

## `/api/radar/stop`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `radar_stop`  
**Description**: يُوقِف محرك رادار السوق. if request.method == "OPTIONS": return jsonify({}), 200 try: return jsonify({"status": "success", **_radar.stop()}) except Exception as e: return jsonify({"status": "error", "  

## `/api/radar/status`
**Methods**: `GET`  
**Handler**: `radar_status`  
**Description**: حالة الرادار + إحصائيات قاعدة بيانات السوق. try: return jsonify({"status": "success", "radar": _radar.status()}) except Exception as e: return jsonify({"status": "error", "message": str(e)}), 500  

## `/api/radar/data`
**Methods**: `GET`  
**Handler**: `radar_data`  
**Description**: يُعيد سجلات السوق المجمَّعة. Query params: location, asset_type, source, hours_back, limit (≤500), offset  

## `/api/radar/parse`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `radar_parse_text`  
**Description**: يُحلِّل نصاً عقارياً حراً ويُضيفه إلى قاعدة البيانات. Body: { "text": "شقة 120م في التجمع بـ 3 مليون" }  

## `/api/radar/heatmap`
**Methods**: `GET`  
**Handler**: `radar_heatmap`  
**Description**: بيانات خريطة الأسعار (PPM مُجمَّعة حسب الموقع). Query: asset_type (default: شقة)  

## `/api/iaao`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `iaao_compute`  
**Description**: يحسب مقاييس IAAO (ASR / COD / PRD / PRB) وإشارات المرور. Body option A — قائمتان مباشرتان: { "assessed_values": [float,...], "sale_prices": [float,...], location": "...", "sector": "residential", "per  

## `/api/iaao/health`
**Methods**: `GET`  
**Handler**: `iaao_health`  

## `/api/training/register`
**Methods**: `POST`  
**Handler**: `training_register`  
**Description**: Receives the master template metadata from the frontend Training Clip. try: body        = request.get_json(force=True, silent=True) or {} filename    = str(body.get("filename", "")).strip() sheet_text  

## `/api/training/status`
**Methods**: `GET`  
**Handler**: `training_status`  
**Description**: Returns the currently registered master template, if any. if _training_registry: return jsonify({"status": "success", "registered": True, "meta": _training_registry}) return jsonify({"status": "succes  

## `/api/price/intelligence`
**Methods**: `POST`  
**Handler**: `price_intelligence_search`  
**Description**: البحث الكامل عن أسعار منطقة محددة. Body (JSON): location    str   — المنطقة (مثل: التجمع الخامس) market      str   — egypt | ksa          (افتراضي: egypt) asset_type  str   — نوع الأصل            (افت  

## `/api/price/trend`
**Methods**: `GET`  
**Handler**: `price_trend`  
**Description**: جلب إشارات الاتجاه الاقتصادي فقط (بدون scraping الأسعار — أسرع). Query params: market    egypt | ksa location  اختياري — لتخصيص البحث  

## `/api/price/excel-table`
**Methods**: `POST`  
**Handler**: `price_excel_table`  
**Description**: يُعيد جدول مقارنة الأسعار بتنسيق JSON جاهز للحقن في شيت Excel. Body (JSON): records   — قائمة سجلات من /api/price/intelligence (اختياري) summary   — ملخص من /api/price/intelligence       (اختياري) loc  

## `/api/price/cache/clear`
**Methods**: `POST`  
**Handler**: `price_cache_clear`  
**Description**: يمسح كاش بروتوكول الرصد اللحظي. return jsonify(_price_intel.clear_cache()) # ═══════════════════════════════════════════════════════════════════════════ # File Upload Endpoints — /api/ingest  /api/upl  

## `/api/ingest`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `ingest_file`  
**Description**: استقبال ملفات Excel / PDF / Word / CSV وتخزينها للمعالجة. if request.method == "OPTIONS": return jsonify({}), 200 if "file" not in request.files: return jsonify({"status": "error", "message": "لم يتم   

## `/api/upload`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `upload_image`  
**Description**: استقبال صور العقارات وتخزينها. if request.method == "OPTIONS": return jsonify({}), 200 if "file" not in request.files: return jsonify({"status": "error", "message": "لم يتم إرسال صورة"}), 400 f = requ  

## `/api/fraud/detect`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `fraud_detect`  
**Description**: POST /api/fraud/detect يُحلّل سعر المتر ويكشف عن احتمالية التلاعب أو الشذوذ. Body: { price_per_meter, location, property_type?, comp_ppms?, area_m2?, total_price? }  

## `/api/geo/risk`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `geo_risk`  
**Description**: POST /api/geo/risk يُحلّل المخاطر الجيوتقنية (التربة، السيول، الزلازل) للموقع. Body: { location, lat?, lng?, property_type? }  

## `/api/demographic/flow`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `demographic_flow`  
**Description**: POST /api/demographic/flow يُحلّل الزحف العمراني وكثافة POI وتوصيات الاستثمار. Body: { location, lat?, lng?, investment_horizon_yr? }  

## `/api/assets`
**Methods**: `GET`  
**Handler**: `assets_list`  
**Description**: GET /api/assets?market=egypt&limit=50 — قائمة الأصول. market = request.args.get("market") limit  = int(request.args.get("limit", 50)) return jsonify({"status": "success", "assets": _am_list(market=mar  

## `/api/assets/register`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `assets_register`  
**Description**: POST /api/assets/register — تسجيل عقار جديد في المحفظة. if request.method == "OPTIONS": return jsonify({}), 200 try: b = request.get_json(force=True) or {} result = _am_register( name          = str(b  

## `/api/assets/<asset_id>`
**Methods**: `GET`  
**Handler**: `assets_get`  
**Description**: GET /api/assets/<id> — بيانات عقار + سجله التاريخي. return jsonify(_am_get(asset_id)) @app.route("/api/assets/<asset_id>/update", methods=["PUT", "POST", "OPTIONS"]) def assets_update(asset_id: str):   

## `/api/assets/<asset_id>/update`
**Methods**: `PUT` / `POST` / `OPTIONS`  
**Handler**: `assets_update`  
**Description**: PUT /api/assets/<id>/update — تحديث قيمة العقار. if request.method == "OPTIONS": return jsonify({}), 200 try: b = request.get_json(force=True) or {} result = _am_update( asset_id        = asset_id,  

## `/api/assets/dashboard`
**Methods**: `GET`  
**Handler**: `assets_dashboard`  
**Description**: GET /api/assets/dashboard?market=egypt — لوحة أداء المحفظة. market = request.args.get("market") return jsonify(_am_dashboard(market=market)) @app.route("/api/assets/<asset_id>", methods=["DELETE"]) de  

## `/api/assets/<asset_id>`
**Methods**: `DELETE`  
**Handler**: `assets_delete`  
**Description**: DELETE /api/assets/<id> — حذف عقار من المحفظة. return jsonify(_am_delete(asset_id)) # ── 5. معالجة الصور الذكية (Auto-Tagging) ──────────────────────────────── import struct  

## `/api/image/analyze`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `image_analyze`  
**Description**: POST /api/image/analyze — multipart/form-data with field 'file'. Response: {status, tag, report_order, has_gps, gps_lat?, gps_lng?, saved_as}  

## `/api/eia/assess`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_eia_assess`  

## `/api/hbu/analyze`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_hbu_analyze`  

## `/api/reit/nav`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_reit_nav`  

## `/api/price-index`
**Methods**: `GET` / `POST` / `OPTIONS`  
**Handler**: `handle_price_index`  

## `/api/mass-appraisal/preview`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_appraisal_preview`  

## `/api/mass-appraisal/run`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_appraisal_run`  

## `/api/mass-appraisal/export-xlsx`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_appraisal_export_xlsx`  

## `/api/mass-appraisal/sales/verify`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_sales_verify`  

## `/api/mass-appraisal/sales/time-adjust`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_sales_time_adjust`  

## `/api/mass-appraisal/sales/adjust`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_sales_adjust`  

## `/api/mass-appraisal/ratio-study/run`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_ratio_study`  

## `/api/mass-appraisal/calibration/preview`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_calibration_preview`  

## `/api/mass-appraisal/calibration/sandbox`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_calibration_sandbox`  

## `/api/mass-appraisal/template-xlsx`
**Methods**: `GET` / `OPTIONS`  
**Handler**: `handle_mass_appraisal_template_xlsx`  
**Description**: Return a blank Mass Appraisal Excel template for download. if request.method == "OPTIONS": return jsonify({}), 200 try: try: from mass_appraisal_template import build_mass_appraisal_template_workbook   

## `/api/mass-appraisal/import-xlsx`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `handle_mass_appraisal_import_xlsx`  
**Description**: Parse an uploaded Mass Appraisal template .xlsx and return normalized JSON preview. No valuation is executed. No files are saved to disk.  

## `/api/comparables/search`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_comparable_search`  
**Description**: Search comparables: PostgreSQL first, JSON search engine fallback. if request.method == "OPTIONS": return jsonify({}), 200 try: data    = request.get_json(force=True) or {} subject = data.get("subject  

## `/api/engines/comparative`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_engine_comparative`  
**Description**: Sales Comparison Approach — applies area/age adjustments to ranked comparables. if request.method == "OPTIONS": return jsonify({}), 200 try: data   = request.get_json(force=True) or {} engine = get_co  

## `/api/engines/cost`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_engine_cost`  
**Description**: Cost Approach — Replacement Cost New minus depreciation plus land value. if request.method == "OPTIONS": return jsonify({}), 200 try: data   = request.get_json(force=True) or {} engine = get_cost_engi  

## `/api/engines/income`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_engine_income`  
**Description**: Income Approach — Direct Capitalization (NOI / Cap Rate). if request.method == "OPTIONS": return jsonify({}), 200 try: data   = request.get_json(force=True) or {} engine = get_income_engine() result =  

## `/api/valuation/full`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_full`  
**Description**: Full valuation pipeline: Phase 4 engines → Phase 5 purpose adapters. Input JSON keys (all optional except subject_property): subject_property       dict   property attributes for search + engines filt  

## `/api/valuation/report`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_report`  
**Description**: Generate an EGVS-compliant 8-sheet Excel report for an asset valuation. Runs the same Phase 4 + Phase 5 pipeline as /api/valuation/full, then applies the Phase 6 AssetAdapter for the requested asset_t  

## `/api/valuation/report/download/<filename>`
**Methods**: `GET`  
**Handler**: `api_valuation_report_download`  
**Description**: Download a previously generated Excel valuation report. <filename> must be the UUID-based name returned by POST /api/valuation/report. The file is served as an attachment with MIME type application/vn  

## `/api/valuation/land`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_land`  
**Description**: Land valuation pipeline: Phase 4 engines → Phase 5 market adapter → Phase 7 LandAdapter. Required body keys: subject_property  dict  — must include property_type (vacant_land / raw_land / development_  

## `/api/valuation/audit`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_audit`  
**Description**: Quality audit endpoint: run ReportQualityAuditor on any Phase 5-7 result JSON. Accepts the serialised form of an AssetValuationResult and returns a full AuditReport without needing the original Python  

## `/api/valuation/portfolio`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_portfolio`  
**Description**: Portfolio valuation endpoint (Phase 11). Accepts a list of pre-valued properties, computes aggregate metrics, and optionally generates an Excel portfolio summary report.  

## `/api/valuation/portfolio/performance`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_portfolio_performance`  
**Description**: Portfolio performance + scenario analysis endpoint (Phase 11.2). Accepts a list of pre-valued properties and optional scenario definitions. Returns stressed metrics, value-at-risk, and IRR estimates a  

## `/api/valuation/batch`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_batch`  
**Description**: Batch valuation endpoint (Phase 12.1). Accepts an array of properties and processes them in a single request. Each property resolves its valuation value via one of three strategies: 1. input_data.valu  

## `/api/valuation/batch/<batch_id>`
**Methods**: `GET` / `OPTIONS`  
**Handler**: `api_valuation_batch_status`  
**Description**: Retrieve a stored batch report by batch_id (Phase 12.3). Returns the completion report registered by POST /api/valuation/batch. Returns 404 if the batch_id is not found (unknown or server restarted).  

## `/api/valuation/batch`
**Methods**: `GET` / `OPTIONS`  
**Handler**: `api_valuation_batch_list`  
**Description**: List recent batches from the in-memory registry (Phase 12.3). Query params: limit  — max results to return (default 20, max 100)  

## `/api/valuation/dcf`
**Methods**: `POST` / `OPTIONS`  
**Handler**: `api_valuation_dcf`  
**Description**: DCF income valuation endpoint (Phase 10). Accepts multi-year cash flow projections and optional sensitivity analysis. Returns NPV-based property value + optional scenario comparison.  

## `/api/enterprise/tenant`
**Methods**: `POST`  
**Handler**: `api_enterprise_create_tenant`  
**Description**: POST /api/enterprise/tenant — create a new tenant organization. try: data = request.get_json(force=True) or {} org_name = (data.get("organization_name") or "").strip() country  = (data.get("country")   

## `/api/enterprise/tenant/<tenant_id>`
**Methods**: `GET`  
**Handler**: `api_enterprise_get_tenant`  
**Description**: GET /api/enterprise/tenant/<tenant_id> — fetch tenant summary. summary = _tenant_manager.get_tenant_summary(tenant_id) if not summary: return jsonify({"status": "error", "message": "Tenant not found"}  

## `/api/enterprise/tenant/<tenant_id>/user`
**Methods**: `POST`  
**Handler**: `api_enterprise_add_user`  
**Description**: POST /api/enterprise/tenant/<tenant_id>/user — add a user to a tenant. try: data      = request.get_json(force=True) or {} email     = (data.get("email") or "").strip() full_name = (data.get("full_nam  

## `/api/enterprise/tenant/<tenant_id>/license`
**Methods**: `GET`  
**Handler**: `api_enterprise_license`  
**Description**: GET /api/enterprise/tenant/<tenant_id>/license — validate subscription. tenant = _tenant_manager.get_tenant(tenant_id) if not tenant: return jsonify({"status": "error", "message": "Tenant not found"})  

## `/api/enterprise/tenant/<tenant_id>/audit`
**Methods**: `GET`  
**Handler**: `api_enterprise_audit`  
**Description**: GET /api/enterprise/tenant/<tenant_id>/audit — fetch audit trail. if not _tenant_manager.get_tenant(tenant_id): return jsonify({"status": "error", "message": "Tenant not found"}), 404 try: limit  = mi  

## `/agent`
**Methods**: `GET`  
**Handler**: `agent_chat_ui`  
**Description**: Serve the Phase 21 Agent Chat UI. chat_html = Path(BASE_DIR).parent / "frontend" / "agent_chat.html if chat_html.exists(): return send_file(str(chat_html)) return "Agent Chat UI not found", 404  

## `/api/agent/chat`
**Methods**: `POST`  
**Handler**: `agent_chat`  
**Description**: POST /api/agent/chat Body: {"message": "run pipeline on <workspace_id>", "workspace_id": "<optional>"} Returns: {"success": bool, "message": str, "data": {...}, "intent": str}  
