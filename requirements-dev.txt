# Development and testing dependencies (not required in production)

# E2E / Playwright
playwright==1.61.0
pytest-playwright==0.8.0

# Async test compatibility (pytest-playwright leaves a running asyncio loop
# that breaks asyncio.run() in subsequent tests; nest-asyncio patches the loop
# to allow nested calls so the full suite runs clean)
nest-asyncio>=1.6.0,<2.0

# MCP bridge tests (test_ph2_coverage_gaps, test_phase_16/18/19/21_e2e)
# import mcp_bridge.py which depends on fastmcp at the top level.
fastmcp>=3.0.0,<4.0

# PDF text extraction — required by 4 tests in test_pdf_certification,
# test_pdf_cost_income_reconciliation, test_pdf_engine, test_pdf_sales_comparison
# that use pytest.importorskip("pdfplumber") to validate PDF content.
pdfplumber>=0.11.10,<0.12

# Coverage reporting — required by --cov=./--cov-report= flags in the
# CI-safe requirements step (.github/workflows/ci-cd.yml).
pytest-cov==7.1.0
